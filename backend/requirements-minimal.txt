# Minimal requirements to get backend running
# Core backend only - avoids packages requiring C++ compilation

fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.0

# Neo4j (pure Python)
neo4j==5.16.0

# For Supabase, we'll use httpx directly (avoiding pyroaring dependency)
httpx==0.26.0

# Data processing
pyyaml==6.0.1
python-dateutil==2.8.2

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
